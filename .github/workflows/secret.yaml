name: Secret Demo

on:
  push:

jobs:
  use-secret:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Print literal secret reference
        run: echo "The secret is \${{ secrets.SECRET }}"
